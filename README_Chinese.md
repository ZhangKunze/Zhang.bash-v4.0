中文版：


本仓库是 Zhang.bash 脚本的4.0版。
提升了体验，升级了功能。
该版本是我在寒假 Github 账户无法登陆时在本地 Git 上改进的脚本。
2.0、3.0、3.2都随时间被改了，我没有它们了。它们都不稳定，且功能不完善。

命令：
```bash
chmod +x ~/Zhang.bash/Zhang.bash && bash ~/Zhang.bash/Zhang.bash
```

```bash
cd ~/Zhang.bash/ && chmod +x Zhang\(Not_good-Editon\).bash && bash /Zhang\(Not_good-Editon\).bash
```

```bash
cd ~/Zhang.bash/ && chmod +x zkz && cp -a zkz /data/data/com.termux/files/usr/bin/zkz
```

```bash
chmod +x ~/Zhang.bash-v4.0/第三个我写的程序 && ~/Zhang.bash-v4.0/第三个我写的程序
```

####由于我是一个小学生，开发脚本只是课余时间，所以顶多能维护一个脚本， 而原版可以让我在课余的时间写更多的代码，而不需要考虑兼容性，所以Zhang(Not_good-Edition).bash不再维护。####

我的 Github : https://github.com/ZhangKunze
